# 模型优化记录

> 更新时间：2026-09-02
> 目的：记录可复现结论和实验止损规则，避免重复进入已经证伪的方向。
> 训练约束：只使用自身完整历史自博弈数据；Pikafish 标签只作固定评测，不进入训练。

## 1. 不可破坏的生产约束

- Arena 启用时，actor 只能发布已经晋升的 champion，失败候选不得污染经验池。
- `actor_publish_interval_updates=5` 只用于未启用 Arena 的初始阶段，不得绕过 champion 门控。
- 不为单次实验向生产代码加入长期配置、兼容层或维护入口。
- 临时实验必须隔离经验池；失败后删除实验源码、候选权重和冗余产物。

## 2. 统一实验流程

1. **明确单一假设**：一次只改变结构、标签、采样或优化器中的一项。
2. **检查数据可靠性**：棋规相关实验必须保留完整 `RuleHistoryEntry`；普通 FEN/replay 特征不能代替重复、长将长捉和六十回合历史。
3. **离线定位**：同时测训练集和按对局、generation 或来源隔离的留出集，禁止同盘局面随机泄漏。
4. **固定集筛选**：记录 raw policy Top-1、search Top-k、value corr/MAE；Pikafish 仅作为外部量尺。
5. **性能门槛**：记录模型大小、NPS 和训练吞吐。没有明确棋力收益时，不接受推理复杂度或明显 NPS 损失。
6. **Arena 筛选**：先 2400 局配对筛选；只有方向明确者才扩大到约 8000 局。最终以棋力和置信区间决定，不以单个离线指标代替。
7. **生产准入**：必须能说明收益来源、成本和回滚点；否则保持原实现。

## 3. 已确认有效

| 改动 | 验证结果 | 成本 | 状态 |
|---|---:|---:|---|
| policy accumulator rank 32→64 | 8000 局得分 51.42%，约 +9.9 Elo | NPS 无可测下降 | 保留 |
| short-value 辅助损失固定 0.05 | 8000 局得分 53.51%，约 +24.4 Elo | 推理零成本，训练约 +9% | 保留 |
| actor generation 元数据修正 | 修复经验来源记录 | 无推理成本 | 保留 |
| Pikafish 固定集按 seed 均匀抽样 | 消除固定 limit 的顺序偏差 | 仅评测 | 保留 |

## 4. 已否决，不原样重复

| 方向 | 关键结果 | 否决原因 |
|---|---:|---|
| value hidden 128 | 48.18%，约 -12.7 Elo | 模型变重且棋力下降 |
| value loss weight 0.25 | 47.57%，约 -16.9 Elo | value 约束减弱导致退化 |
| 全局 policy target sharpening，T=0.8 | 48.625%，约 -9.6 Elo | 固定集局部变化未转化为棋力 |
| 20–27 子力局面增加 10% 采样 | 中局 Top-1 约 +0.2pp，整体退化 | 收益过小且分布偏置 |
| policy loss weight 1.25 | 8000 局 49.95%，约 -0.35 Elo | Adam 基本抵消纯缩放，无收益 |
| 独立 policy LR ×1.5 | 固定评测退化 | 优化速度不是主要瓶颈 |
| move×signature / move×piece pair factor | 固定集无增益 | 新容量未被有效利用 |
| replay 反建局面后重分析 | 未执行 | 缺完整规则历史，标签不可靠 |
| 400→800/1600 深搜教师直接拟合 | 1600 对 400：2400 局 49.667%，约 -2.3 Elo | 标签更收敛，但模型与棋力未吸收 |

## 5. 当前可靠事实

- 同一固定集上，7540→8830 的 value 改善真实：raw MAE 约 0.2635→0.2589，search MAE 约 0.2235→0.2155；policy 只有边缘、混合变化。
- replay 位置重复率约 0.029%，冻结 champion 不等于局面重复，不能据此盲目增加温度或根噪声。
- 400 模拟标签尚未收敛。使用完整历史快照、同 seed，以 6400 为参考：
  - opening Top-1：400/800/1600 = 72.85% / 77.20% / 83.40%。
  - midgame Top-1：69.05% / 74.60% / 80.85%。
- 但 100k 配对快照 policy-only 训练后，1600 教师的 raw/search Top-1 为 37.9%/49.5%，原 8830 为 38.1%/49.6%，Arena 也不胜。
- 因而当前不能把问题简单归结为搜索预算不足。下一步必须区分：
  1. 训练集内是否能吸收深搜差异；
  2. 吸收后能否跨 generation/来源泛化；
  3. 泛化提升能否转化为棋力。

## 6. 当前下一步

- 用完整历史快照按 generation 和来源切分，建立无泄漏 train/validation。
- 对 400 与 1600 教师分别记录 train/validation 的 CE、KL 和 Top-1，不先改生产结构。
- 若训练集也学不会：定位 policy 表达或优化瓶颈。
- 若训练集能学会但留出集不行：定位特征泛化与标签噪声。
- 若留出集提升但 Arena 不升：判定训练目标与棋力错位，停止追逐 policy pp。

## 7. 吸收与泛化诊断（2026-09-01）

使用完整历史快照按时间切分：generation≤7800 为训练来源，其中 position hash 的 20% 作同年代留出；generation≥8010 作未来留出。最终为训练 72,879、同年代留出 18,228、未来留出 8,893。

以 1600 模拟访问分布为目标：

| 模型 | 训练 KL / Top-1 | 同年代留出 KL / Top-1 | 未来留出 KL / Top-1 |
|---|---:|---:|---:|
| 原 8830 | 0.48885 / 60.70% | 0.54832 / 57.53% | 0.55307 / 56.81% |
| 1600 教师，1 epoch | 0.48365 / 60.85% | 0.54804 / 57.41% | 0.55276 / 56.61% |
| 1600 教师，4 epoch | 0.47215 / 61.48% | 0.54814 / 57.31% | 0.55318 / 56.45% |

结论：当前网络可以少量记忆深搜教师，但没有同年代或跨 generation 泛化。不能把问题简单归结为总容量不足，也不能用训练 CE 下降作为准入依据。

跨 seed 稳定性排除了共享根噪声假象：400/1600/6400 的 Top-1 跨 seed 一致率分别为 96.88%/96.73%/97.75%，但完整访问分布的对称 KL 为 0.2346/0.0837/0.0358。Top-1 稳定，soft target 尾部明显更不稳定。

400→1600 且 1600 与 6400 一致的稳定修正有 551 个，反向退化 119 个；其中 410 个（74.4%）是安静着改为另一安静着。原始 prior 在这些修正中匹配 400 为 287 次，匹配 1600 仅 81 次，说明低预算搜索仍受当前 prior 锚定。

下一实验只检验一个假设：1600 稳定 Top-1 的低权重辅助监督能否比完整 soft target 更好泛化。不得把它扩展成全局 sharpening；未通过留出集直接终止，不进入生产。

该假设已否决。相同 1600 标签下，以 `90% soft + 10% top1` 训练：1 epoch 的同年代/未来 Top-1 相对纯 soft 分别下降 0.093pp/0.169pp；4 epoch 分别下降 0.175pp/0.045pp。`25% top1` 没有改善。训练集的微小增益仍不能跨局面迁移，因此不进入 Arena，也不在生产加入 top1 辅助 loss。

至此，静态离线蒸馏的搜索预算、soft target、top1 辅助和简单优化缩放均无棋力证据。下一项只允许做隔离的短期 on-policy 闭环 A/B：同一 champion、独立经验池、相同更新数与训练参数，唯一变量为 400/1600 simulations；不得接触生产池。该实验用于判断深教师是否必须通过多轮策略迭代才能产生收益。

该闭环 A/B 已完成并否决。两组从同一 8830 开始，各自独立生成并训练 5×20k 样本，学习率固定 5.3e-5，actor 每轮只发布本实验最新 learner；唯一变量为 simulations。固定集上 400/1600 的 raw policy Top-1 为 37.6%/37.5%，search Top-1 为 49.4%/49.7%，search value MAE 为 0.2067/0.2094。随后 2400 局配对 Arena 中，1600 对 400 为 922/998/480，得分 48.417%，95% CI 46.935%–49.898%，约 -11 Elo。

最终结论：训练 simulations 保持 400。更深搜索的标签收敛性改善没有通过静态蒸馏、稳定 top1 辅助或短期 on-policy 闭环转化为棋力；除非未来模型表示发生实质变化，不再重复 800/1600 教师路线。

## 8. Policy accumulator 容量曲线（2026-09-02）

rank64 已相对 rank32 通过 8000 局验证。为检查当前吸收瓶颈是否仍由 accumulator 容量造成，测试 rank96：从同一 8830 精确迁移，新增 move 因子零初始化；rank64/rank96 使用相同 replay、10×100k 样本、seed、LR 和 batch。

- 最终训练 Policy CE：rank64 2.051825，rank96 2.051781，改善约 0.002%。
- 固定集 raw Top-1：38.0%→38.2%；search Top-1：49.8%→49.6%，Top-2/Top-4 各下降 0.1pp。
- 固定 400 模拟 startpos 基准：231,699→218,170 simulations/s，NPS 下降约 5.84%。

结论：rank96 明确否决，不进入 Arena；policy accumulator 在 rank64 已出现容量收益饱和。除非训练目标或表示方式发生变化，不再继续线性增加 rank。

## 9. 深搜修正与 short-value 权重曲线（2026-09-02）

对 551 个“400 与 1600 不同、且 1600 与 6400 一致”的稳定修正，比较两个候选走后的一步网络 value：value 支持深搜选择 245 次，支持被纠正的 400 选择 306 次，平均 `deep-shallow=-0.0189`。其中 410 个安静着→安静着修正里为 175/235，平均差 -0.0247。说明 value 本身也偏向 prior/400，不能靠简单 value→policy 共享解决；深搜增量主要来自多步搜索。

随后固定同一 8830、replay、10×100k 样本和训练顺序，比较 short-value loss weight 0.025/0.05/0.10：

- 固定集上 0.025 相对 0.05：search Top-1 49.7%→50.1%，raw/search value MAE 各改善约 0.0002，但 Top-2/Top-4 略低。
- 0.10 的训练 value CE、raw policy 和 value 指标总体略差，不进入 Arena。
- 0.025 对 0.05 的 2400 局 Arena 为 948/931/521，得分 50.354%，95% CI 49.182%–51.526%，约 +2.5 Elo，无显著提升。

结论：保留 short-value loss weight 0.05。0.025/0.10 均不进入生产；0.05 附近已是平坦区，不再做更细的小数点扫描。

## 10. 安静着 FullThreat 特征探针（2026-09-02）

为检验 policy 是否缺少走后战术关系，只做冻结模型的离线信息探针，不改生产网络。完整 opening/midgame 池共抽取 100,000 个带 `RuleHistoryEntry` 的快照；同 seed 搜索 400/1600/6400，仅保留 `400 != 1600 == 6400` 且两个候选均为安静着的 9,864 个稳定修正。特征为候选走后差分：己/敌攻击覆盖、规则合法着数、被将状态、14×14 攻击者/被攻击者棋子类型关系，以及 256 维空间关系哈希。

按 generation 严格切分：≤7330 训练 8,369，7501–7800 校准正则 582，≥8010 最终检验 913。逻辑回归结果：

| 条件特征 | 未来准确率 | BCE | AUC | 相对 raw 准确率增量（配对 bootstrap 95% CI） |
|---|---:|---:|---:|---:|
| raw prior 差 | 77.00% | 0.5184 | 0.8306 | 基线 |
| raw + 全局统计 | 76.89% | 0.5178 | 0.8311 | -0.11pp [-0.66,+0.33] |
| raw + 棋子类型 FullThreat | 76.78% | 0.5331 | 0.8271 | -0.22pp [-1.75,+1.31] |
| raw + 类型及空间 FullThreat | 76.78% | 0.5300 | 0.8197 | -0.22pp [-2.41,+1.97] |

这里的 77% 是在“已知样本属于深搜稳定纠错”的条件下学习反转 raw 偏差所得，不能解释为可部署的 policy 准确率；有效判断只看新增特征相对同条件基线的跨代增量。

结论：首轮 20,000 局面上类型特征出现的约 +2.7pp 在完整池中消失，属于小样本波动。走后 FullThreat 统计没有提供可泛化增量，空间关系还表现出更强过拟合。该假设否决，不增加 FullThreat stack、空间哈希或走后攻击扫描，也不进入训练/Arena。

## 11. 冻结 policy 内部表示探针（2026-09-02）

沿用第 10 节完全相同的 100,000 个快照、9,864 个稳定安静着修正和 generation 切分，冻结 8830 模型，导出两个候选的 116 维内部交互差：PSQ consequence 32 维、全局×走法 context 16 维、rank64 accumulator 交互，以及 bias/sparse/tactical 项。只训练带 L2 的线性读出；≤7330 训练、7501–7800 选择正则、≥8010 检验。

| 线性读出输入 | 未来准确率 | BCE | AUC | 相对 raw 准确率增量（generation 整组 bootstrap 95% CI） |
|---|---:|---:|---:|---:|
| raw prior 差 | 77.00% | 0.5184 | 0.8306 | 基线 |
| raw + PSQ 32 | 76.89% | 0.5172 | 0.8327 | -0.11pp [-1.90,+1.80] |
| raw + context 16 | 76.01% | 0.5180 | 0.8310 | -0.99pp [-2.04,-0.40] |
| raw + accumulator 64 | 75.68% | 0.5236 | 0.8244 | -1.31pp [-3.04,+0.75] |
| raw + context/accumulator | 75.14% | 0.5230 | 0.8256 | -1.86pp [-3.16,-0.18] |
| raw + 全部 116 维 | 76.01% | 0.5222 | 0.8278 | -0.99pp [-2.68,+1.19] |

与第 10 节相同，77% 只是在“已知它是深搜纠错”的条件样本上学习 raw 的反向偏差，不是可部署准确率。有效结论是：现有候选内部表示经过最简单的重新加权后，在未来 generation 没有提供 raw 之外的稳定信息；rank64 与 context 联合反而显著下降。

结论：当前稳定安静着纠错不是 policy 最终线性 head 没有读好，也不是继续增大 accumulator rank 可以解决。瓶颈位于现有一层局面/走法表示之前，或来自必须经多步搜索才能得到、无法由该静态表示泛化的目标。不得据此增加 head、重加权现有通道或继续扩 rank；若继续 policy 研究，应先改变可学习监督的定义或验证多步信息的最低成本表示，而不是再加容量。

## 12. Value-only PVS/Alpha-Beta 最小基线（2026-09-02）

为检验能否直接用经典剪枝替换 MCTS，实现了隔离的最小基线：当前 8830 value-only NNUE、双视角增量 accumulator、迭代加深、PVS、置换表、历史/killer排序、吃子静态搜索，并保留完整 `RuleHistoryEntry`。增量前进/撤销与全量刷新逐着校验一致。MCTS关闭根噪声；先单线程测量 MCTS 时间，再给PVS等时节点预算，Arena按节点并行，避免墙钟线程竞争偏置。

| 对照预算 | MCTS耗时 | PVS平均深度/节点 | PVS对MCTS（胜/负/和） | 得分 |
|---|---:|---:|---:|---:|
| 1600 simulations | 约8.8–9.5ms | 约3.0层 / 6.5k | 20/70/10 | 25.0% |
| 6400 simulations | 约37.7ms | 约3.8层 / 22.3k | 18/71/11 | 23.5% |

固定局面上，两者 value 相关系数约 0.93–0.95，但最佳着一致率只有 52%–58%。更长预算没有缩小实战差距，因此不扩大对局。

结论仅限于“当前网络 + 最小PVS”的直接替换：明确否决，不进入生产。它不证明成熟的 Pikafish/Stockfish 级经典搜索不如 MCTS；该级实现还需要增量网络专用布局、成熟TT/棋规处理、SEE、LMR、null move、singular extension及大量调参，已经是独立搜索引擎工程，而非低复杂度模型优化。当前项目不应通过继续堆一个残缺剪枝器来解决 policy/MCTS 训练闭环问题。

## 13. 20450 后平台与回滚 A/B（2026-09-03）

生产在 update 21447 安全暂停。`best.safetensors` 仍为 20450，actor 的 replay generation 也始终为 20450，因此被拒 learner 没有污染经验池；但代码确认 learner 在 Arena Reject 后不会回滚，而会继续从被拒权重训练。

先读取 TensorBoard 原始事件而非观察平滑曲线。20460–21440 共 99 次 Arena 对当前冠军的平均得分为 49.229%，前后半段为 49.285%/49.173%；线性斜率为每 1000 update -0.064pp，标准误 0.352pp，`t=-0.18`。五个连续 200-update 块的均值为 49.308%、49.367%、48.959%、49.160%、49.354%。因此不存在持续单调崩坏，状态是围绕略弱均值振荡的平台；21440 的 48.438% 是真实的单次低点，但不能外推为持续退化。

权重相对 best-20450 的 L2 距离则从 20460 的 0.0458% 几乎严格单调增至 21440 的 0.6560%。漂移最大的可训练张量为 `policy_accumulator_move`（相对 L2 2.86%），同期固定 Pikafish policy/value 指标没有相应改善。说明低学习率仍在代理损失的平坦方向累计移动，而不是参数停止更新。

为检验“恢复 best 并重置优化器”是否有效，在两个隔离目录中复制完全相同的暂停 replay、opening/midgame pool、冠军、progress 和随机种子，各运行 21447–21547；生产模型与经验池未修改。唯一初始差异：A 使用暂停时 current-21447，B 使用 best-20450。两边 actor 都固定为 champion-20450。

| 分支 | 10 次 Arena 均值 | 固定集 search Top-1 | raw prior Top-1 | raw/search value MAE |
|---|---:|---:|---:|---:|
| A：current 继续 | 49.740% | 54.08% | 41.95% | 0.33311 / 0.30388 |
| B：best 重启 | 49.630% | 54.43% | 41.66% | 0.33346 / 0.30411 |

Arena 配对差 `B-A=-0.11pp`，配对标准误 0.374pp，`t=-0.29`，没有可辨识收益。恢复 best 并重训的假设否决，不应覆盖生产 current 后盲目续跑。B 的 search Top-1 虽高 0.35pp，但 raw Top-1 低 0.29pp、value 略差且 Arena 不升，再次证明单个离线 pp 不能作为棋力代理。

评估还存在两个统计缺陷。第一，同一 Arena update 的置信区间只计入该批配对对局的条件方差，没有计入每个 update 更换开局集合和搜索随机种子的外层批次方差；A/B 同一 update 的得分相关系数为 0.39，且两组在 21500 同时跌至 47.9%/48.4%，可见批次效应。第二，平台期每 10 update 重复一次 `z=1.96` 的单次准入检验；若真实等强，单次误晋级约 2.5%，独立近似下连续 100 次至少一次误晋级概率约 92%，即使相关性会降低该数值，重复检验与冠军选择偏差仍不可忽略。

当前结论：这是“固定冠军自博弈闭环 + 代理目标不再转化为棋力 + 准入评估过度频繁且低估外层方差”的系统平台，不是模型文件损坏、经验池污染、优化器偶发坏状态或已证实的结构容量上限。下一实验必须先修正测量：减少 Arena 频率、增加每次独立开局批次规模并提高准入置信门槛；在可靠测量建立前，不再改模型结构、policy loss、value loss 或搜索算法。

## 14. Policy regret 与权重平均探针（2026-09-03）

新增零训练影响的自博弈诊断：`raw_top1_regret`、按 raw prior 加权的 `raw_policy_regret`，以及 top1 regret 超过 0.05/0.10/0.20 的比例。regret 只在已访问子节点上按 MCTS child Q 计算，避免用未访问节点的默认 Q。全量 154 项测试通过，服务器 fast profile 编译及实际一轮自博弈写入 TensorBoard 均通过。

在隔离 current 轨迹的 update 21548，平均 raw top1/policy regret 为 0.0503/0.0954；top1 regret 超过 0.05/0.10/0.20 的比例为 23.96%/12.98%/5.90%。随后继续隔离运行 100 update，在 10 个 Arena 点上检验这些指标能否预测棋力。该段 Arena 均值仅 48.52%；raw top1 regret、raw policy regret、blunder>0.05 与 Arena 的同期相关分别为 +0.24/+0.31/+0.43，方向没有支持“regret 越低棋力越强”，且样本点不足以建立稳定关系。结论：保留 regret 作为诊断，不据此进行 replay 重采样或 loss 加权。

同时测试 stochastic weight averaging：将 20460–21440 的 50 个 checkpoint 等权平均，再沿 best-20450→平均权重插值 25%/50%/75%/100%。使用生产一致的 400 simulations、policy temperature 1.55、1000 个 OBK 开局交换红黑，共 2000 局/配对。各插值模型对 best-20450 的得分为 49.90%/49.33%/48.88%/49.18%，没有候选超过 best；完整矩阵还出现 50% SWA 胜 25% SWA 51.95%、但对 best 仍只有 49.33% 的局部非传递。SWA 否决，不替换 champion。

生产仍暂停并保留 best-20450。服务器 Arena 配置已备份到 `eval/chineseai.azloop.pre-gate-v2.toml`，待恢复时采用单变量的 gate-v2：interval 10→100、OBK/random positions 各 1000→8000、promotion z 1.96→3.09。每 1000 update 由约 100 次、单次 2.5% 的重复假阳性暴露，降为约 10 次、单次约 0.1%；Arena 总对局预算约为原来的 80%，不会增加长期评估成本。

## 15. Policy 盲点定向探索剂量实验（2026-09-03）

检验一种不引入外部数据和模型结构的最小数据闭环：按对局抽样，在首次出现 `raw_top1_regret > 0.10` 时强制走 raw prior top1、随后仍由冠军搜索；每局最多一次。强制根节点的 value weight 置零，避免故意探索着污染 value，policy 仍学习该根的 MCTS visits。所有实验从完全相同的 best-20450、暂停 replay/pool 和 progress-21447 开始，actor 固定为冠军且禁止中途晋级，运行 101 updates。

实际强制对局率随配置 2%/5%/10% 分别为 1.78%/4.54%/9.12%。首种子的最终四模型循环赛（每对 4000 局）显示倒 U 形：2%/5%/10% 对 control 分别为 51.45%/52.64%/48.63%；10% 另用 8000 局复核为 47.94%，明确退化。由于单条训练轨迹不能估计算法外层方差，继续对 5% 做两个独立随机种子复验，每个均用 8000 局直接对照。

| 5% 复验种子 | 对 control 得分 | 约 Elo |
|---|---:|---:|
| 20260421 | 52.64% | +18.3 |
| 20260422 | 50.04% | +0.3 |
| 20260423 | 47.13% | -20.0 |

三种子效应为 +2.64/+0.04/-2.87pp，均值 -0.06pp，种子间标准差 2.76pp，按三次独立实验计算 `t=-0.04`，95% t 区间约 -6.91pp～+6.78pp。固定同一最终模型的大量对局只能压低内层对局误差，不能消除训练轨迹外层方差；首轮 +18 Elo 是不可复现的轨迹收益。

结论：强制进入 raw-policy 盲点没有稳定棋力增益，10% 还会明显退化；方案否决，不进入生产。实验代码已撤回，避免增加长期维护面，仅保留零训练影响的 policy regret 诊断。今后任何训练机制候选至少采用三种子同起点 A/B，并以每种子最终直接对弈的效应作为独立观测；单种子大量对局只用于筛选，不能作为上线证据。

## 16. 高 regret 根的深搜 policy 重分析（2026-09-03）

测试不改变自博弈落子的 teacher-label 方案：先用原 400 simulations 完成实际选着；仅当该根 `raw_top1_regret > 0.10` 时，另做一次无 Dirichlet 噪声的 1600 simulations 搜索，只用其 visits 替换该样本的 policy target。实际落子、value/short-value target 和后续轨迹均不读取重分析结果。所有实验从同一 best-20450、暂停 replay/pool 和 progress-21447 开始，actor 固定为冠军，运行 101 updates。

约 13.1% 的根触发重分析，平均搜索预算由 400 增至约 609 simulations/根；端到端 update 时间由约 3.1 秒增至约 5.0 秒，计算成本上升约 61%。三个随机种子的最终模型分别与对应 control 进行 8000 局直接对弈：

| 种子 | teacher 对 control 得分 | 约 Elo |
|---|---:|---:|
| 20260421 | 52.34% | +16.3 |
| 20260422 | 49.28% | -5.0 |
| 20260423 | 47.50% | -17.4 |

三种子效应为 +2.34/-0.72/-2.50pp，均值 -0.29pp，种子间标准差 2.45pp，`t=-0.21`，95% t 区间约 -6.34pp～+5.79pp，不支持稳定增益。固定 1000 局面 Pikafish 标签集上，teacher 相对 control 的 raw prior Top-1 为 0.0/+0.7/+0.8pp，search Top-1 为 -0.4/-0.3/+1.7pp；离线 policy 指标平均略升但未转化为棋力，第三种子甚至在 search Top-1 +1.7pp 时实战约 -17 Elo。

结论：选择性深搜监督否决，不进入生产。它同时增加约 61% 自博弈成本并放大 target 尖锐度，却没有跨轨迹稳定棋力收益；实验实现已撤回。该结果进一步确认当前瓶颈不是“400-sim policy 标签不够深”这一单因子，继续提高搜索监督深度或追逐固定集 policy pp 都不是有效方向。
